# This is a basic workflow that is manually triggered

name: Vanguard C check
on:
  push:
    branches-ignore:
      - '**-coverage-report'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    
