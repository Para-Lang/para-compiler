/**
 * A simple program showing the primary functionality of Para.
 */
import { Print } from "pbl-io";

class Blueprint {
  int primaryValue;

  init(int value) {
    this.primaryValue = value;
  }

  noret SetValue() {
    this.initValue = 4;
    Print(f"Set value to {this.primaryValue}");
  }

  int GetValue() {
    return this.primaryValue;
  }
}

entry status Main() {
  // Simple variable
  int num = 4;

  // Get the type of the variable
  type typeOfNum = GetType(num);

  // Print out the type representation
  Print(typeOfNum);

  // Simple class initialisation
  Blueprint instance = new BluePrint(5);
  instance.SetValue(50); // noret
  instance.GetValue(50); // -> 50
}
